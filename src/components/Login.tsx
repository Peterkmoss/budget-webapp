import React, { Component } from 'react'

class Login extends Component {
    constructor(props: {}) {
        super(props)
    }

    render(): JSX.Element {
        return (
            <h1>Login</h1>
        );
    }
}

export default Login